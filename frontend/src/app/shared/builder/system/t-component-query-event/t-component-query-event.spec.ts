import { TComponentQueryEvent } from './t-component-query-event';

describe('TComponentQueryEvent', () => {
  it('should create an instance', () => {
    expect(new TComponentQueryEvent()).toBeTruthy();
  });
});
